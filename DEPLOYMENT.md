# Cloudflare deployment

This site is built by Astro and deployed as static assets on Cloudflare Workers.

```sh
npm ci
npm run deploy
```

The custom domain is managed in Cloudflare. The repository contains no credentials or DNS records.

// URL sets that are intentionally indexable. Keep this shared so the sitemap,
// static route generation, and internal links agree.

export const INDEXABLE_ALTERNATIVE_SLUGS = new Set([
  'hermes',
  'secure-openclaw',
  'openfang',
]);
